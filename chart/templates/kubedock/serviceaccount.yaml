{{- if .Values.kubedock.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ok8s.fullname" . }}-kubedock
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ok8s.labels" . | nindent 4 }}
    app.kubernetes.io/component: kubedock
{{- end }}
