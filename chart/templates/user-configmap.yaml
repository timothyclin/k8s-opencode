{{- if eq .Values.mode "multi" }}
{{- $root := . }}
{{- range $index, $user := .Values.users }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ok8s.fullname" $root }}-user-{{ $user.name }}-config
  namespace: {{ $root.Release.Namespace }}
  labels:
    {{- include "ok8s.labels" $root | nindent 4 }}
    app.kubernetes.io/user: {{ $user.name | quote }}
data:
  opencode.jsonc: |
    {{- include "ok8s.opencodeUserConfig" (dict "root" $root "user" $user) | nindent 4 }}
  oh-my-opencode.jsonc: |
    {{- include "ok8s.omoUserConfig" (dict "root" $root "user" $user) | nindent 4 }}
{{- end }}
{{- end }}
