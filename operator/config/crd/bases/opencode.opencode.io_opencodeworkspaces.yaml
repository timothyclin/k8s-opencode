---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: opencodeworkspaces.opencode.opencode.io
spec:
  group: opencode.opencode.io
  names:
    kind: OpenCodeWorkspace
    listKind: OpenCodeWorkspaceList
    plural: opencodeworkspaces
    singular: opencodeworkspace
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OpenCodeWorkspace is the Schema for the opencodeworkspaces API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of OpenCodeWorkspace
            properties:
              email:
                description: Email is the identity email address for the workspace
                  owner.
                pattern: ^[^@]+@[^@]+$
                type: string
              kubedock:
                description: Kubedock configures the optional kubedock sidecar.
                properties:
                  enabled:
                    description: Enabled turns on the kubedock sidecar.
                    type: boolean
                  resources:
                    description: Resources defines CPU/memory requests and limits
                      for kubedock.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                type: object
              mcp:
                description: MCP configures Model Context Protocol servers.
                properties:
                  laptopServers:
                    description: LaptopServers lists MCP servers running on the user's
                      laptop (via Tailscale egress).
                    items:
                      description: LaptopMCPServer defines an MCP server running on
                        the user's laptop.
                      properties:
                        enabled:
                          default: true
                          description: Enabled controls whether this MCP server is
                            active.
                          type: boolean
                        name:
                          description: Name is a unique identifier for this MCP server.
                          minLength: 1
                          type: string
                        port:
                          description: Port is the TCP port where the MCP server listens.
                          format: int32
                          maximum: 65535
                          minimum: 1
                          type: integer
                        tailscaleFqdn:
                          description: TailscaleFQDN is the MagicDNS hostname of the
                            laptop.
                          type: string
                        tailscaleIP:
                          description: TailscaleIP is the Tailscale IP address of
                            the laptop.
                          type: string
                      required:
                      - name
                      - port
                      type: object
                    type: array
                  remote:
                    description: Remote lists remote MCP servers accessible via HTTP.
                    items:
                      description: RemoteMCPServer defines a remote MCP server configuration.
                      properties:
                        enabled:
                          default: true
                          description: Enabled controls whether this MCP server is
                            active.
                          type: boolean
                        headers:
                          additionalProperties:
                            type: string
                          description: Headers are optional HTTP headers to include
                            in MCP requests.
                          type: object
                        name:
                          description: Name is a unique identifier for this MCP server.
                          minLength: 1
                          type: string
                        oauth:
                          description: OAuth configures optional OAuth2 authentication.
                          properties:
                            clientId:
                              description: ClientID is the OAuth2 client ID.
                              type: string
                            clientSecret:
                              description: ClientSecret is the OAuth2 client secret.
                              type: string
                            tokenUrl:
                              description: TokenURL is the OAuth2 token endpoint.
                              type: string
                          type: object
                        url:
                          description: URL is the HTTP endpoint for the MCP server.
                          minLength: 1
                          type: string
                      required:
                      - name
                      - url
                      type: object
                    type: array
                type: object
              namespacePrefix:
                default: oc
                description: |-
                  NamespacePrefix is the prefix for the workspace namespace.
                  If username is "timothy" and prefix is "oc", namespace will be "oc-timothy".
                  Defaults to "oc".
                type: string
              plugins:
                description: |-
                  Plugins configures npm plugins to install in the workspace.
                  Default plugins (oh-my-opencode@latest, @tarquinen/opencode-dcp@latest, superpowers)
                  are always included unless explicitly disabled.
                properties:
                  enabled:
                    default: true
                    description: |-
                      Enabled controls whether default plugins are loaded.
                      Default plugins: oh-my-opencode@latest, @tarquinen/opencode-dcp@latest, superpowers
                    type: boolean
                  npm:
                    description: |-
                      NPM is a list of additional npm packages to install.
                      Example: ["my-plugin@latest", "@scope/plugin@1.0.0"]
                    items:
                      type: string
                    type: array
                type: object
              providers:
                description: Providers configures one or more AI providers.
                properties:
                  anthropic:
                    description: Anthropic config.
                    properties:
                      apiKeySecretRef:
                        description: APIKeySecretRef references a Secret containing
                          the provider API key.
                        properties:
                          name:
                            description: Name is the Secret name.
                            type: string
                          namespace:
                            description: Namespace is the Secret namespace.
                            type: string
                        type: object
                      enabled:
                        description: Enabled toggles whether this provider is enabled
                          for the workspace.
                        type: boolean
                      model:
                        default: claude-sonnet-4-20250514
                        description: Model is the Anthropic model identifier.
                        type: string
                    type: object
                  openai:
                    description: OpenAI config.
                    properties:
                      apiKeySecretRef:
                        description: APIKeySecretRef references a Secret containing
                          the provider API key.
                        properties:
                          name:
                            description: Name is the Secret name.
                            type: string
                          namespace:
                            description: Namespace is the Secret namespace.
                            type: string
                        type: object
                      enabled:
                        description: Enabled toggles whether this provider is enabled
                          for the workspace.
                        type: boolean
                      model:
                        description: Model is the provider-specific model identifier.
                        type: string
                    type: object
                  openrouter:
                    description: OpenRouter config.
                    properties:
                      apiKeySecretRef:
                        description: APIKeySecretRef references a Secret containing
                          the provider API key.
                        properties:
                          name:
                            description: Name is the Secret name.
                            type: string
                          namespace:
                            description: Namespace is the Secret namespace.
                            type: string
                        type: object
                      enabled:
                        description: Enabled toggles whether this provider is enabled
                          for the workspace.
                        type: boolean
                      model:
                        description: Model is the provider-specific model identifier.
                        type: string
                    type: object
                type: object
              resources:
                description: Resources defines CPU/memory requests and limits for
                  the workspace workload.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              serverPassword:
                description: |-
                  ServerPassword is the password for the OpenCode HTTP server.
                  If not provided, a random password will be generated.
                  The password is stored in the workspace-secrets Secret.
                type: string
              skills:
                description: Skills configures skills (reusable prompt templates and
                  tool configurations).
                properties:
                  config:
                    description: Config is a list of inline skill configurations as
                      raw JSON.
                    x-kubernetes-preserve-unknown-fields: true
                  npm:
                    description: NPM is a list of npm packages providing skills.
                    items:
                      type: string
                    type: array
                type: object
              storage:
                description: Storage defines persistent volume sizes for the workspace.
                properties:
                  data:
                    anyOf:
                    - type: integer
                    - type: string
                    description: Data is the PVC size for ~/.opencode.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  storageClassName:
                    description: StorageClassName is the StorageClass used for PVCs.
                      Empty means the cluster default.
                    type: string
                  workspace:
                    anyOf:
                    - type: integer
                    - type: string
                    description: Workspace is the PVC size for /workspace.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                type: object
              tailscale:
                description: Tailscale configures tailnet ingress and optional egress
                  connectivity.
                properties:
                  egress:
                    description: Egress configures optional egress connectivity (e.g.,
                      to laptop MCP servers).
                    properties:
                      enabled:
                        description: Enabled turns on egress connectivity.
                        type: boolean
                      laptopHostname:
                        description: LaptopHostname is the MagicDNS hostname for the
                          user's laptop.
                        type: string
                      mcpPorts:
                        description: MCPPorts lists MCP services exposed from the
                          laptop via ExternalName Services.
                        items:
                          description: MCPPort defines a single MCP server port.
                          properties:
                            name:
                              description: Name is a short identifier for the MCP
                                server.
                              minLength: 1
                              type: string
                            port:
                              description: Port is the TCP port exposed by the MCP
                                server.
                              format: int32
                              maximum: 65535
                              minimum: 1
                              type: integer
                          required:
                          - name
                          - port
                          type: object
                        type: array
                      tags:
                        description: Tags are tags applied to the egress proxy identity.
                        items:
                          type: string
                        type: array
                    type: object
                  ingressTags:
                    description: IngressTags are tags applied to the Tailscale ingress
                      identity.
                    items:
                      type: string
                    type: array
                type: object
            required:
            - email
            type: object
          status:
            description: status defines the observed state of OpenCodeWorkspace
            properties:
              aclSnippet:
                description: ACLSnippet is a recommended ACL policy snippet for tailnet
                  configuration.
                type: string
              conditions:
                description: |-
                  conditions represent the current state of the OpenCodeWorkspace resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              ingressHostname:
                description: IngressHostname is the tailnet hostname used to reach
                  the workspace.
                type: string
              message:
                description: Message is a human-readable status message.
                type: string
              namespace:
                description: Namespace is the namespace created/managed for this workspace.
                type: string
              phase:
                description: Phase is a high-level summary of the workspace lifecycle.
                enum:
                - Pending
                - Creating
                - Reconciling
                - Running
                - Failed
                - Terminating
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
